runner:
  textlint:
    cmd: npx textlint -f compact doc/*.md
    errorformat:
      - "%f:%l:%c: %m"
      - "%f: line %l, col %c, %m"
  redpendoc:
    cmd: redpen -c config/redpen-conf-ja.xml -L ja -l 9999 -r plain doc/*.md 2>/dev/null
    name: redpen
    errorformat:
      - "%Z%f:%l: %m"
